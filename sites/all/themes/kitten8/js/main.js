// Import libs
import $ from 'jquery';

(function () {
  // Run when DOM is ready
  $(function () {

  });

  // Run when DOM is changed
  Drupal.behaviors.kitten8 = {
    attach() {

    }
  };
})();
